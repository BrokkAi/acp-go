// Code generated by acpgen from ACP JSON Schema release schema-v1.21.0 (wire types). DO NOT EDIT.

package schema

import (
	"encoding/json"
	"fmt"
)

// Meta is the reserved _meta channel for extension data.
type Meta map[string]any

// Authentication-related capabilities supported by the agent.
type AgentAuthCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Whether the agent supports the logout method.
	// Optional. Omitted or null both mean the agent does not advertise support.
	// Supplying {} means the agent supports the logout method.
	Logout *LogoutCapabilities `json:"logout,omitempty"`
}

// Capabilities supported by the agent.
// Advertised during initialization to inform the client about
// available features and content types.
// See protocol docs: Agent Capabilities
type AgentCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Authentication-related capabilities supported by the agent.
	Auth *AgentAuthCapabilities `json:"auth,omitempty"`
	// Whether the agent supports session/load.
	LoadSession *bool `json:"loadSession,omitempty"`
	// MCP capabilities supported by the agent.
	MCPCapabilities *McpCapabilities `json:"mcpCapabilities,omitempty"`
	// Prompt capabilities supported by the agent.
	PromptCapabilities *PromptCapabilities `json:"promptCapabilities,omitempty"`
	// Session lifecycle and prompt capabilities advertised by the agent.
	SessionCapabilities *SessionCapabilities `json:"sessionCapabilities,omitempty"`
}

// Optional annotations for the client. The client can use annotations to inform how objects are used or displayed
type Annotations struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Intended recipients for this content, such as the user or assistant.
	Audience []Role `json:"audience,omitempty"`
	// Timestamp indicating when the underlying resource was last modified.
	LastModified *string `json:"lastModified,omitempty"`
	// Relative importance of this content when clients choose what to surface.
	Priority *float64 `json:"priority,omitempty"`
}

// Audio provided to or from an LLM.
type AudioContent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Base64-encoded media payload.
	Data string `json:"data"`
	// MIME type describing the encoded media payload.
	MimeType string `json:"mimeType"`
}

// Authentication capabilities supported by the client.
// Advertised during initialization to inform the agent which authentication
// method types the client can handle. This governs opt-in types that require
// additional client-side support.
type AuthCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Whether the client supports terminal authentication methods.
	// The client should set this to true only when it can reproduce the
	// configured agent invocation in an interactive terminal. When true, the
	// agent may include terminal entries in its authentication methods.
	Terminal *bool `json:"terminal,omitempty"`
}

// Describes an available authentication method.
// The type field acts as the discriminator in the serialized JSON form.
// When no type is present, the method is treated as agent.
type AuthMethodKind string

const (
	AuthMethodKindTerminal AuthMethodKind = "terminal"
)

// AuthMethod is a tagged union discriminated by "type".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type AuthMethod struct {
	Kind     AuthMethodKind      `json:"-"`
	Agent    *AuthMethodAgent    `json:"-"`
	Terminal *AuthMethodTerminal `json:"-"`
}

func (v AuthMethod) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.Agent != nil {
		set++
	}
	if v.Terminal != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "AuthMethod", set)
	}
	if v.Agent != nil {
		payload, err := json.Marshal(v.Agent)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("AuthMethod payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Terminal != nil {
		if tag, err := json.Marshal("terminal"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Terminal)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("AuthMethod payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *AuthMethod) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = AuthMethod{}
	var tag string
	if raw, ok := raws["type"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("AuthMethod tag: %w", err)
		}
	}
	switch tag {
	case "":
		v.Agent = new(AuthMethodAgent)
		return json.Unmarshal(data, v.Agent)
	case "terminal":
		v.Kind = AuthMethodKindTerminal
		v.Terminal = new(AuthMethodTerminal)
		return json.Unmarshal(data, v.Terminal)
	default:
		return fmt.Errorf("AuthMethod: unknown type tag %q", tag)
	}
}

// Agent handles authentication itself through authenticate.
// This is the default authentication method type.
type AuthMethodAgent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional description providing more details about this authentication method.
	Description *string `json:"description,omitempty"`
	// Unique identifier for this authentication method.
	ID AuthMethodId `json:"id"`
	// Human-readable name of the authentication method.
	Name string `json:"name"`
}

// Typed identifier used for auth method values on the wire.
type AuthMethodId string

// Terminal-based authentication method.
// The client runs the configured agent program as a separate interactive
// process for the user to authenticate via a TUI. Agents MUST advertise this
// method only when the client enabled its terminal authentication capability.
// A zero exit status signals success; any other termination signals failure.
// The client MUST NOT pass this method to authenticate.
type AuthMethodTerminal struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Additional arguments to append to the configured agent invocation for terminal auth.
	Args []string `json:"args,omitempty"`
	// Optional description providing more details about this authentication method.
	Description *string `json:"description,omitempty"`
	// Additional environment variables to set on the configured agent invocation for terminal auth.
	// These values override same-named variables in the base launch configuration.
	Env map[string]string `json:"env,omitempty"`
	// Unique identifier for this authentication method.
	ID AuthMethodId `json:"id"`
	// Human-readable name of the authentication method.
	Name string `json:"name"`
}

// Request parameters for the authenticate method.
// Specifies which authentication method to use.
type AuthenticateRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the authentication method to use.
	// Must be one of the methods advertised in the initialize response.
	MethodID AuthMethodId `json:"methodId"`
}

// Response to the authenticate method.
type AuthenticateResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Information about a command.
type AvailableCommand struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Human-readable description of what the command does.
	Description string `json:"description"`
	// Input for the command if required
	Input *AvailableCommandInput `json:"input,omitempty"`
	// Command name (e.g., create_plan, research_codebase).
	Name string `json:"name"`
}

// The input specification for a command.
type AvailableCommandInput = UnstructuredCommandInput

// Available commands are ready or have changed
type AvailableCommandsUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Commands the agent can execute
	AvailableCommands []AvailableCommand `json:"availableCommands"`
}

// Binary resource contents.
type BlobResourceContents struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Base64-encoded bytes for a binary resource payload.
	Blob string `json:"blob"`
	// MIME type describing the encoded media payload.
	MimeType *string `json:"mimeType,omitempty"`
	// URI associated with this resource or media payload.
	URI string `json:"uri"`
}

// Capabilities for boolean session configuration options.
// Supplying {} means the client supports boolean session configuration options.
type BooleanConfigOptionCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Schema for boolean properties in an elicitation form.
type BooleanPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Default value.
	// Optional. Omitted and null are equivalent and mean no default value is provided.
	Default *bool `json:"default,omitempty"`
	// Human-readable description.
	// Optional. Omitted and null are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Optional title for the property.
	// Optional. Omitted and null are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// Notification to cancel ongoing operations for a session.
// See protocol docs: Cancellation
type CancelNotification struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the session to cancel operations for.
	SessionID SessionId `json:"sessionId"`
}

// Notification to cancel an ongoing request.
// See protocol docs: Cancellation
type CancelRequestNotification struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the request to cancel.
	RequestID RequestId `json:"requestId"`
}

// Capabilities supported by the client.
// Advertised during initialization to inform the agent about
// available features and methods.
// See protocol docs: Client Capabilities
type ClientCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Authentication capabilities supported by the client.
	// Determines which authentication method types the agent may include
	// in its InitializeResponse.
	Auth *AuthCapabilities `json:"auth,omitempty"`
	// Elicitation capabilities supported by the client.
	// Determines which elicitation modes the agent may use.
	// Optional. Omitted or null both mean the client does not advertise
	// elicitation support.
	Elicitation *ElicitationCapabilities `json:"elicitation,omitempty"`
	// File system capabilities supported by the client.
	// Determines which file operations the agent can request.
	Fs *FileSystemCapabilities `json:"fs,omitempty"`
	// Session-related capabilities supported by the client.
	// Optional. Omitted or null both mean the client does not advertise any
	// session-related extensions.
	Session *ClientSessionCapabilities `json:"session,omitempty"`
	// Whether the Client support all terminal/* methods.
	Terminal *bool `json:"terminal,omitempty"`
}

// A JSON-RPC notification object.
type ClientNotification struct {
	// The notification method name.
	Method string `json:"method"`
	// Method-specific notification parameters.
	Params json.RawMessage `json:"params,omitempty"`
}

// A JSON-RPC request object.
type ClientRequest struct {
	// The request id used to correlate the matching response.
	ID RequestId `json:"id"`
	// The method name to invoke.
	Method string `json:"method"`
	// Method-specific request parameters.
	Params json.RawMessage `json:"params,omitempty"`
}

// Session-related capabilities supported by the client.
type ClientSessionCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Config option capabilities supported by the client.
	// Omitted or null both mean the client does not advertise support for any
	// config option extensions.
	ConfigOptions *SessionConfigOptionsCapabilities `json:"configOptions,omitempty"`
}

// Request parameters for closing an active session.
// If supported, the agent must cancel any ongoing work related to the session
// (treat it as if session/cancel was called) and then free up any resources
// associated with the session.
// Only available if the Agent supports the sessionCapabilities.close capability.
type CloseSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the session to close.
	SessionID SessionId `json:"sessionId"`
}

// Response from closing a session.
type CloseSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Notification sent by the agent when a URL-based elicitation is complete.
type CompleteElicitationNotification struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the elicitation that completed.
	ElicitationID ElicitationId `json:"elicitationId"`
}

// Session configuration options have been updated.
type ConfigOptionUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The full set of configuration options and their current values.
	ConfigOptions []SessionConfigOption `json:"configOptions"`
}

// Standard content block (text, images, resources).
type Content struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The actual content block.
	Content ContentBlock `json:"content"`
}

// Content blocks represent displayable information in the Agent Client Protocol.
// They provide a structured way to handle various types of user-facing content—whether
// it's text from language models, images for analysis, or embedded resources for context.
// Content blocks appear in:
// - User prompts sent via session/prompt
// - Language model output streamed through session/update notifications
// - Progress updates and results from tool calls
// This structure is compatible with the Model Context Protocol (MCP), enabling
// agents to seamlessly forward content from MCP tool outputs without transformation.
// See protocol docs: Content
type ContentBlockKind string

const (
	ContentBlockKindText         ContentBlockKind = "text"
	ContentBlockKindImage        ContentBlockKind = "image"
	ContentBlockKindAudio        ContentBlockKind = "audio"
	ContentBlockKindResourceLink ContentBlockKind = "resource_link"
	ContentBlockKindResource     ContentBlockKind = "resource"
)

// ContentBlock is a tagged union discriminated by "type".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type ContentBlock struct {
	Kind         ContentBlockKind  `json:"-"`
	Text         *TextContent      `json:"-"`
	Image        *ImageContent     `json:"-"`
	Audio        *AudioContent     `json:"-"`
	ResourceLink *ResourceLink     `json:"-"`
	Resource     *EmbeddedResource `json:"-"`
}

func (v ContentBlock) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.Text != nil {
		set++
	}
	if v.Image != nil {
		set++
	}
	if v.Audio != nil {
		set++
	}
	if v.ResourceLink != nil {
		set++
	}
	if v.Resource != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "ContentBlock", set)
	}
	if v.Text != nil {
		if tag, err := json.Marshal("text"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Text)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ContentBlock payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Image != nil {
		if tag, err := json.Marshal("image"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Image)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ContentBlock payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Audio != nil {
		if tag, err := json.Marshal("audio"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Audio)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ContentBlock payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.ResourceLink != nil {
		if tag, err := json.Marshal("resource_link"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.ResourceLink)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ContentBlock payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Resource != nil {
		if tag, err := json.Marshal("resource"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Resource)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ContentBlock payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *ContentBlock) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = ContentBlock{}
	var tag string
	if raw, ok := raws["type"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("ContentBlock tag: %w", err)
		}
	}
	switch tag {
	case "text":
		v.Kind = ContentBlockKindText
		v.Text = new(TextContent)
		return json.Unmarshal(data, v.Text)
	case "image":
		v.Kind = ContentBlockKindImage
		v.Image = new(ImageContent)
		return json.Unmarshal(data, v.Image)
	case "audio":
		v.Kind = ContentBlockKindAudio
		v.Audio = new(AudioContent)
		return json.Unmarshal(data, v.Audio)
	case "resource_link":
		v.Kind = ContentBlockKindResourceLink
		v.ResourceLink = new(ResourceLink)
		return json.Unmarshal(data, v.ResourceLink)
	case "resource":
		v.Kind = ContentBlockKindResource
		v.Resource = new(EmbeddedResource)
		return json.Unmarshal(data, v.Resource)
	default:
		return fmt.Errorf("ContentBlock: unknown type tag %q", tag)
	}
}

// A streamed item of content
type ContentChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// A single item of content
	Content ContentBlock `json:"content"`
	// A unique identifier for the message this chunk belongs to.
	// All chunks belonging to the same message share the same messageId.
	// A change in messageId indicates a new message has started.
	MessageID *MessageId `json:"messageId,omitempty"`
}

// Cost information for a session.
type Cost struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Total cumulative cost for session.
	Amount float64 `json:"amount"`
	// ISO 4217 currency code (e.g., "USD", "EUR").
	Currency string `json:"currency"`
}

// Request from the agent to elicit structured user input.
// The agent sends this to the client to request information from the user,
// either via a form or by directing them to a URL.
// Elicitations are tied to a session (optionally a tool call) or a request.
type CreateElicitationRequestKind string

const (
	CreateElicitationRequestKindForm CreateElicitationRequestKind = "form"
	CreateElicitationRequestKindUrl  CreateElicitationRequestKind = "url"
)

// CreateElicitationRequest is a tagged union discriminated by "mode".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type CreateElicitationRequest struct {
	Kind CreateElicitationRequestKind `json:"-"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// A human-readable message describing what input is needed.
	Message string                         `json:"message"`
	Form    *ElicitationFormMode           `json:"-"`
	URL     *ElicitationUrlMode            `json:"-"`
	Other   *CreateElicitationRequestOther `json:"-"`
}

// CreateElicitationRequestOther preserves an unrecognized CreateElicitationRequest payload verbatim.
type CreateElicitationRequestOther struct{ Raw json.RawMessage }

func (v CreateElicitationRequestOther) MarshalJSON() ([]byte, error) { return v.Raw, nil }

// createElicitationRequestShadow mirrors CreateElicitationRequest without its union methods, for encoding common fields.
type createElicitationRequestShadow CreateElicitationRequest

func (v CreateElicitationRequest) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	if common, err := json.Marshal(createElicitationRequestShadow(v)); err != nil {
		return nil, err
	} else if len(common) > 2 {
		if err := json.Unmarshal(common, &obj); err != nil {
			return nil, err
		}
	}
	set := 0
	if v.Form != nil {
		set++
	}
	if v.URL != nil {
		set++
	}
	if v.Other != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "CreateElicitationRequest", set)
	}
	if v.Form != nil {
		if tag, err := json.Marshal("form"); err != nil {
			return nil, err
		} else {
			obj["mode"] = tag
		}
		payload, err := json.Marshal(v.Form)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("CreateElicitationRequest payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.URL != nil {
		if tag, err := json.Marshal("url"); err != nil {
			return nil, err
		} else {
			obj["mode"] = tag
		}
		payload, err := json.Marshal(v.URL)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("CreateElicitationRequest payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Other != nil {
		return v.Other.Raw, nil
	}
	return json.Marshal(obj)
}

func (v *CreateElicitationRequest) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = CreateElicitationRequest{}
	if err := json.Unmarshal(data, (*createElicitationRequestShadow)(v)); err != nil {
		return err
	}
	var tag string
	if raw, ok := raws["mode"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("CreateElicitationRequest tag: %w", err)
		}
	}
	switch tag {
	case "form":
		v.Kind = CreateElicitationRequestKindForm
		v.Form = new(ElicitationFormMode)
		return json.Unmarshal(data, v.Form)
	case "url":
		v.Kind = CreateElicitationRequestKindUrl
		v.URL = new(ElicitationUrlMode)
		return json.Unmarshal(data, v.URL)
	default:
		v.Other = &CreateElicitationRequestOther{Raw: append(json.RawMessage(nil), data...)}
		return nil
	}
}

// Response from the client to an elicitation request.
type CreateElicitationResponseKind string

const (
	CreateElicitationResponseKindAccept  CreateElicitationResponseKind = "accept"
	CreateElicitationResponseKindDecline CreateElicitationResponseKind = "decline"
	CreateElicitationResponseKindCancel  CreateElicitationResponseKind = "cancel"
)

// CreateElicitationResponse is a tagged union discriminated by "action".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type CreateElicitationResponse struct {
	Kind CreateElicitationResponseKind `json:"-"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta    Meta                              `json:"_meta,omitempty"`
	Accept  *ElicitationAcceptAction          `json:"-"`
	Decline *CreateElicitationResponseDecline `json:"-"`
	Cancel  *CreateElicitationResponseCancel  `json:"-"`
	Other   *CreateElicitationResponseOther   `json:"-"`
}

// CreateElicitationResponseOther preserves an unrecognized CreateElicitationResponse payload verbatim.
type CreateElicitationResponseOther struct{ Raw json.RawMessage }

func (v CreateElicitationResponseOther) MarshalJSON() ([]byte, error) { return v.Raw, nil }

type CreateElicitationResponseDecline struct {
}

type CreateElicitationResponseCancel struct {
}

// createElicitationResponseShadow mirrors CreateElicitationResponse without its union methods, for encoding common fields.
type createElicitationResponseShadow CreateElicitationResponse

func (v CreateElicitationResponse) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	if common, err := json.Marshal(createElicitationResponseShadow(v)); err != nil {
		return nil, err
	} else if len(common) > 2 {
		if err := json.Unmarshal(common, &obj); err != nil {
			return nil, err
		}
	}
	set := 0
	if v.Accept != nil {
		set++
	}
	if v.Decline != nil {
		set++
	}
	if v.Cancel != nil {
		set++
	}
	if v.Other != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "CreateElicitationResponse", set)
	}
	if v.Accept != nil {
		if tag, err := json.Marshal("accept"); err != nil {
			return nil, err
		} else {
			obj["action"] = tag
		}
		payload, err := json.Marshal(v.Accept)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("CreateElicitationResponse payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Decline != nil {
		if tag, err := json.Marshal("decline"); err != nil {
			return nil, err
		} else {
			obj["action"] = tag
		}
		payload, err := json.Marshal(v.Decline)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("CreateElicitationResponse payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Cancel != nil {
		if tag, err := json.Marshal("cancel"); err != nil {
			return nil, err
		} else {
			obj["action"] = tag
		}
		payload, err := json.Marshal(v.Cancel)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("CreateElicitationResponse payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Other != nil {
		return v.Other.Raw, nil
	}
	return json.Marshal(obj)
}

func (v *CreateElicitationResponse) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = CreateElicitationResponse{}
	if err := json.Unmarshal(data, (*createElicitationResponseShadow)(v)); err != nil {
		return err
	}
	var tag string
	if raw, ok := raws["action"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("CreateElicitationResponse tag: %w", err)
		}
	}
	switch tag {
	case "accept":
		v.Kind = CreateElicitationResponseKindAccept
		v.Accept = new(ElicitationAcceptAction)
		return json.Unmarshal(data, v.Accept)
	case "decline":
		v.Kind = CreateElicitationResponseKindDecline
		v.Decline = new(CreateElicitationResponseDecline)
		return json.Unmarshal(data, v.Decline)
	case "cancel":
		v.Kind = CreateElicitationResponseKindCancel
		v.Cancel = new(CreateElicitationResponseCancel)
		return json.Unmarshal(data, v.Cancel)
	default:
		v.Other = &CreateElicitationResponseOther{Raw: append(json.RawMessage(nil), data...)}
		return nil
	}
}

// Request to create a new terminal and execute a command.
type CreateTerminalRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Array of command arguments.
	Args []string `json:"args,omitempty"`
	// The command to execute.
	Command string `json:"command"`
	// Working directory for the command. Must be an absolute path.
	Cwd *string `json:"cwd,omitempty"`
	// Environment variables for the command.
	Env []EnvVariable `json:"env,omitempty"`
	// Maximum number of output bytes to retain.
	// When the limit is exceeded, the Client truncates from the beginning of the output
	// to stay within the limit.
	// The Client MUST ensure truncation happens at a character boundary to maintain valid
	// string output, even if this means the retained output is slightly less than the
	// specified limit.
	OutputByteLimit *uint64 `json:"outputByteLimit,omitempty"`
	// The session ID for this request.
	SessionID SessionId `json:"sessionId"`
}

// Response containing the ID of the created terminal.
type CreateTerminalResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The unique identifier for the created terminal.
	TerminalID TerminalId `json:"terminalId"`
}

// The current mode of the session has changed
// See protocol docs: Session Modes
type CurrentModeUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the current mode
	CurrentModeID SessionModeId `json:"currentModeId"`
}

// Request parameters for deleting an existing session from session/list.
// Only available if the Agent supports the sessionCapabilities.delete capability.
type DeleteSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the session to delete.
	SessionID SessionId `json:"sessionId"`
}

// Response from deleting a session.
type DeleteSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// A diff representing file modifications.
// Shows changes to files in a format suitable for display in the client UI.
// See protocol docs: Content
type Diff struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The new content after modification.
	NewText string `json:"newText"`
	// The original content (None for new files).
	OldText *string `json:"oldText,omitempty"`
	// The absolute file path being modified.
	Path string `json:"path"`
}

// The user accepted the elicitation and provided content.
type ElicitationAcceptAction struct {
	// The user-provided content, if any, as an object matching the requested schema.
	Content map[string]ElicitationContentValue `json:"content,omitempty"`
}

// Elicitation capabilities supported by the client.
type ElicitationCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Whether the client supports form-based elicitation.
	// Optional. Omitted and null are equivalent and mean form support is not advertised.
	// Supplying {} explicitly advertises form support.
	Form *ElicitationFormCapabilities `json:"form,omitempty"`
	// Whether the client supports URL-based elicitation.
	// Optional. Omitted or null both mean the client does not advertise support.
	// Supplying {} means the client supports URL-based elicitation.
	URL *ElicitationUrlCapabilities `json:"url,omitempty"`
}

// Allowed wire representations for [ElicitationContentValue].
type ElicitationContentValue any

// Form-based elicitation capabilities.
// Supplying {} means the client supports form-based elicitation.
type ElicitationFormCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Form-based elicitation mode where the client renders a form from the provided schema.
// ElicitationFormMode is a shape-disambiguated union. Exactly one variant pointer must be
// set; decoding probes each variant's required wire keys in order.
type ElicitationFormMode struct {
	Session *ElicitationSessionScope `json:"-"`
	Request *ElicitationRequestScope `json:"-"`
}

func (v ElicitationFormMode) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.Session != nil {
		set++
	}
	if v.Request != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "ElicitationFormMode", set)
	}
	if v.Session != nil {
		payload, err := json.Marshal(v.Session)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationFormMode payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Request != nil {
		payload, err := json.Marshal(v.Request)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationFormMode payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *ElicitationFormMode) UnmarshalJSON(data []byte) error {
	*v = ElicitationFormMode{}
	if hasKeys(data, "sessionId") {
		v.Session = new(ElicitationSessionScope)
		return json.Unmarshal(data, v.Session)
	} else if hasKeys(data, "requestId") {
		v.Request = new(ElicitationRequestScope)
		return json.Unmarshal(data, v.Request)
	}
	return fmt.Errorf("ElicitationFormMode: payload matches no variant")
}

func hasKeys(data []byte, keys ...string) bool {
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return false
	}
	for _, k := range keys {
		if _, ok := obj[k]; !ok {
			return false
		}
	}
	return true
}

func arrayHasKeys(data []byte, keys ...string) bool {
	var items []json.RawMessage
	if err := json.Unmarshal(data, &items); err != nil {
		return false
	}
	if len(items) == 0 {
		return true // an empty array fits the first variant
	}
	return hasKeys(items[0], keys...)
}

// Unique identifier for an elicitation.
type ElicitationId string

// Property schema for elicitation form fields.
// Each variant corresponds to a JSON Schema "type" value.
// Single-select enums use the String variant with enum or oneOf set.
// Multi-select enums use the Array variant.
type ElicitationPropertySchemaKind string

const (
	ElicitationPropertySchemaKindString  ElicitationPropertySchemaKind = "string"
	ElicitationPropertySchemaKindNumber  ElicitationPropertySchemaKind = "number"
	ElicitationPropertySchemaKindInteger ElicitationPropertySchemaKind = "integer"
	ElicitationPropertySchemaKindBoolean ElicitationPropertySchemaKind = "boolean"
	ElicitationPropertySchemaKindArray   ElicitationPropertySchemaKind = "array"
)

// ElicitationPropertySchema is a tagged union discriminated by "type".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type ElicitationPropertySchema struct {
	Kind    ElicitationPropertySchemaKind   `json:"-"`
	String  *StringPropertySchema           `json:"-"`
	Number  *NumberPropertySchema           `json:"-"`
	Integer *IntegerPropertySchema          `json:"-"`
	Boolean *BooleanPropertySchema          `json:"-"`
	Array   *MultiSelectPropertySchema      `json:"-"`
	Other   *ElicitationPropertySchemaOther `json:"-"`
}

// ElicitationPropertySchemaOther preserves an unrecognized ElicitationPropertySchema payload verbatim.
type ElicitationPropertySchemaOther struct{ Raw json.RawMessage }

func (v ElicitationPropertySchemaOther) MarshalJSON() ([]byte, error) { return v.Raw, nil }

func (v ElicitationPropertySchema) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.String != nil {
		set++
	}
	if v.Number != nil {
		set++
	}
	if v.Integer != nil {
		set++
	}
	if v.Boolean != nil {
		set++
	}
	if v.Array != nil {
		set++
	}
	if v.Other != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "ElicitationPropertySchema", set)
	}
	if v.String != nil {
		if tag, err := json.Marshal("string"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.String)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationPropertySchema payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Number != nil {
		if tag, err := json.Marshal("number"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Number)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationPropertySchema payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Integer != nil {
		if tag, err := json.Marshal("integer"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Integer)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationPropertySchema payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Boolean != nil {
		if tag, err := json.Marshal("boolean"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Boolean)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationPropertySchema payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Array != nil {
		if tag, err := json.Marshal("array"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Array)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationPropertySchema payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Other != nil {
		return v.Other.Raw, nil
	}
	return json.Marshal(obj)
}

func (v *ElicitationPropertySchema) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = ElicitationPropertySchema{}
	var tag string
	if raw, ok := raws["type"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("ElicitationPropertySchema tag: %w", err)
		}
	}
	switch tag {
	case "string":
		v.Kind = ElicitationPropertySchemaKindString
		v.String = new(StringPropertySchema)
		return json.Unmarshal(data, v.String)
	case "number":
		v.Kind = ElicitationPropertySchemaKindNumber
		v.Number = new(NumberPropertySchema)
		return json.Unmarshal(data, v.Number)
	case "integer":
		v.Kind = ElicitationPropertySchemaKindInteger
		v.Integer = new(IntegerPropertySchema)
		return json.Unmarshal(data, v.Integer)
	case "boolean":
		v.Kind = ElicitationPropertySchemaKindBoolean
		v.Boolean = new(BooleanPropertySchema)
		return json.Unmarshal(data, v.Boolean)
	case "array":
		v.Kind = ElicitationPropertySchemaKindArray
		v.Array = new(MultiSelectPropertySchema)
		return json.Unmarshal(data, v.Array)
	default:
		v.Other = &ElicitationPropertySchemaOther{Raw: append(json.RawMessage(nil), data...)}
		return nil
	}
}

// Request-scoped elicitation, tied to a specific JSON-RPC request outside of a session
// (e.g., during auth/configuration phases before any session is started).
type ElicitationRequestScope struct {
	// The request this elicitation is tied to.
	RequestID RequestId `json:"requestId"`
}

// Type-safe elicitation schema for requesting structured user input.
// This represents a JSON Schema object with primitive-typed properties,
// as required by the elicitation specification.
type ElicitationSchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional description of what this schema represents.
	// Optional. Omitted and null are equivalent and mean no schema description is provided.
	Description *string `json:"description,omitempty"`
	// Property definitions (must be primitive types).
	Properties map[string]ElicitationPropertySchema `json:"properties,omitempty"`
	// List of required property names.
	// Optional. Omitted and null are equivalent and mean no property names are required.
	Required []string `json:"required,omitempty"`
	// Optional title for the schema.
	// Optional. Omitted and null are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
	// Type discriminator. Always "object".
	Type *ElicitationSchemaType `json:"type,omitempty"`
}

// Type discriminator for elicitation schemas.
type ElicitationSchemaType string

const (
	// Object schema type.
	ElicitationSchemaTypeObject ElicitationSchemaType = "object"
)

// Session-scoped elicitation, optionally tied to a specific tool call.
// When tool_call_id is set, the elicitation is tied to a specific tool call.
// This is useful when an agent receives an elicitation from an MCP server
// during a tool call and needs to redirect it to the user.
type ElicitationSessionScope struct {
	// The session this elicitation is tied to.
	SessionID SessionId `json:"sessionId"`
	// Optional tool call within the session.
	// Optional. Omitted and null are equivalent and mean the elicitation is scoped to the
	// session without a specific tool call.
	ToolCallID *ToolCallId `json:"toolCallId,omitempty"`
}

// URL-based elicitation capabilities.
// Supplying {} means the client supports URL-based elicitation.
type ElicitationUrlCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// URL-based elicitation mode where the client directs the user to a URL.
// ElicitationUrlMode is a shape-disambiguated union. Exactly one variant pointer must be
// set; decoding probes each variant's required wire keys in order.
type ElicitationUrlMode struct {
	Session *ElicitationSessionScope `json:"-"`
	Request *ElicitationRequestScope `json:"-"`
}

func (v ElicitationUrlMode) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.Session != nil {
		set++
	}
	if v.Request != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "ElicitationUrlMode", set)
	}
	if v.Session != nil {
		payload, err := json.Marshal(v.Session)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationUrlMode payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Request != nil {
		payload, err := json.Marshal(v.Request)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ElicitationUrlMode payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *ElicitationUrlMode) UnmarshalJSON(data []byte) error {
	*v = ElicitationUrlMode{}
	if hasKeys(data, "sessionId") {
		v.Session = new(ElicitationSessionScope)
		return json.Unmarshal(data, v.Session)
	} else if hasKeys(data, "requestId") {
		v.Request = new(ElicitationRequestScope)
		return json.Unmarshal(data, v.Request)
	}
	return fmt.Errorf("ElicitationUrlMode: payload matches no variant")
}

// The contents of a resource, embedded into a prompt or tool call result.
type EmbeddedResource struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Embedded resource payload, either text or binary data.
	Resource EmbeddedResourceResource `json:"resource"`
}

// Resource content that can be embedded in a message.
// EmbeddedResourceResource is a shape-disambiguated union. Exactly one variant pointer must be
// set; decoding probes each variant's required wire keys in order.
type EmbeddedResourceResource struct {
	TextResourceContents *TextResourceContents `json:"-"`
	BlobResourceContents *BlobResourceContents `json:"-"`
}

func (v EmbeddedResourceResource) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.TextResourceContents != nil {
		set++
	}
	if v.BlobResourceContents != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "EmbeddedResourceResource", set)
	}
	if v.TextResourceContents != nil {
		payload, err := json.Marshal(v.TextResourceContents)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("EmbeddedResourceResource payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.BlobResourceContents != nil {
		payload, err := json.Marshal(v.BlobResourceContents)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("EmbeddedResourceResource payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *EmbeddedResourceResource) UnmarshalJSON(data []byte) error {
	*v = EmbeddedResourceResource{}
	if hasKeys(data, "text", "uri") {
		v.TextResourceContents = new(TextResourceContents)
		return json.Unmarshal(data, v.TextResourceContents)
	} else if hasKeys(data, "blob", "uri") {
		v.BlobResourceContents = new(BlobResourceContents)
		return json.Unmarshal(data, v.BlobResourceContents)
	}
	return fmt.Errorf("EmbeddedResourceResource: payload matches no variant")
}

// A titled enum option with a const value, human-readable title, and optional description.
type EnumOption struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The constant value for this option.
	Const string `json:"const"`
	// Human-readable description.
	// Optional. Omitted and null are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Human-readable title for this option.
	Title string `json:"title"`
}

// An environment variable to set when launching an MCP server.
type EnvVariable struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The name of the environment variable.
	Name string `json:"name"`
	// The value to set for the environment variable.
	Value string `json:"value"`
}

// JSON-RPC error object.
// Represents an error that occurred during method execution, following the
// JSON-RPC 2.0 error object specification with optional additional data.
// See protocol docs: JSON-RPC Error Object
type Error struct {
	// A number indicating the error type that occurred.
	// This must be an integer as defined in the JSON-RPC specification.
	Code ErrorCode `json:"code"`
	// Optional primitive or structured value that contains additional information about the error.
	// This may include debugging information or context-specific details.
	Data json.RawMessage `json:"data,omitempty"`
	// A string providing a short description of the error.
	// The message should be limited to a concise single sentence.
	Message string `json:"message"`
}

// Predefined error codes for common JSON-RPC and ACP-specific errors.
// These codes follow the JSON-RPC 2.0 specification for standard errors
// and use the reserved range (-32000 to -32099) for protocol-specific errors.
type ErrorCode int64

const (
	// Parse error: Invalid JSON was received by the server.
	// An error occurred on the server while parsing the JSON text.
	ErrorCodeParseError ErrorCode = -32700
	// Invalid request: The JSON sent is not a valid Request object.
	ErrorCodeInvalidRequest ErrorCode = -32600
	// Method not found: The method does not exist or is not available.
	ErrorCodeMethodNotFound ErrorCode = -32601
	// Invalid params: Invalid method parameter(s).
	ErrorCodeInvalidParams ErrorCode = -32602
	// Internal error: Internal JSON-RPC error.
	// Reserved for implementation-defined server errors.
	ErrorCodeInternalError ErrorCode = -32603
	// Request cancelled: Execution of the method was aborted either due to a cancellation request from the caller or
	// because of resource constraints or shutdown.
	ErrorCodeRequestCancelled ErrorCode = -32800
	// Authentication required: Authentication is required before this operation can be performed.
	ErrorCodeAuthenticationRequired ErrorCode = -32000
	// Resource not found: A given resource, such as a file, was not found.
	ErrorCodeResourceNotFound ErrorCode = -32002
)

// Allows the Agent to send an arbitrary notification that is not part of the ACP spec.
// Extension notifications provide a way to send one-way messages for custom functionality
// while maintaining protocol compatibility.
// See protocol docs: Extensibility
type ExtNotification struct {
}

// Allows for sending an arbitrary request that is not part of the ACP spec.
// Extension methods provide a way to add custom functionality while maintaining
// protocol compatibility.
// See protocol docs: Extensibility
type ExtRequest struct {
}

// Allows for sending an arbitrary response to an [ExtRequest] that is not part of the ACP spec.
// Extension methods provide a way to add custom functionality while maintaining
// protocol compatibility.
// See protocol docs: Extensibility
type ExtResponse struct {
}

// File system capabilities that a client may support.
// See protocol docs: FileSystem
type FileSystemCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Whether the Client supports fs/read_text_file requests.
	ReadTextFile *bool `json:"readTextFile,omitempty"`
	// Whether the Client supports fs/write_text_file requests.
	WriteTextFile *bool `json:"writeTextFile,omitempty"`
}

// An HTTP header to set when making requests to the MCP server.
type HttpHeader struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The name of the HTTP header.
	Name string `json:"name"`
	// The value to set for the HTTP header.
	Value string `json:"value"`
}

// An image provided to or from an LLM.
type ImageContent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Base64-encoded media payload.
	Data string `json:"data"`
	// MIME type describing the encoded media payload.
	MimeType string `json:"mimeType"`
	// URI associated with this resource or media payload.
	URI *string `json:"uri,omitempty"`
}

// Metadata about the implementation of the client or agent.
// Describes the name and version of an ACP implementation, with an optional
// title for UI representation.
type Implementation struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Intended for programmatic or logical use, but can be used as a display
	// name fallback if title isn’t present.
	Name string `json:"name"`
	// Intended for UI and end-user contexts — optimized to be human-readable
	// and easily understood.
	// If not provided, the name should be used for display.
	Title *string `json:"title,omitempty"`
	// Version of the implementation. Can be displayed to the user or used
	// for debugging or metrics purposes. (e.g. "1.0.0").
	Version string `json:"version"`
}

// Request parameters for the initialize method.
// Sent by the client to establish connection and negotiate capabilities.
// See protocol docs: Initialization
type InitializeRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Capabilities supported by the client.
	ClientCapabilities *ClientCapabilities `json:"clientCapabilities,omitempty"`
	// Information about the Client name and version sent to the Agent.
	// Note: in future versions of the protocol, this will be required.
	ClientInfo *Implementation `json:"clientInfo,omitempty"`
	// The latest protocol version supported by the client.
	ProtocolVersion ProtocolVersion `json:"protocolVersion"`
}

// Response to the initialize method.
// Contains the negotiated protocol version and agent capabilities.
// See protocol docs: Initialization
type InitializeResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Capabilities supported by the agent.
	AgentCapabilities *AgentCapabilities `json:"agentCapabilities,omitempty"`
	// Information about the Agent name and version sent to the Client.
	// Note: in future versions of the protocol, this will be required.
	AgentInfo *Implementation `json:"agentInfo,omitempty"`
	// Authentication methods supported by the agent.
	AuthMethods []AuthMethod `json:"authMethods,omitempty"`
	// The protocol version the client specified if supported by the agent,
	// or the latest protocol version supported by the agent.
	// The client should disconnect, if it doesn't support this version.
	ProtocolVersion ProtocolVersion `json:"protocolVersion"`
}

// Schema for integer properties in an elicitation form.
type IntegerPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Default value.
	// Optional. Omitted and null are equivalent and mean no default value is provided.
	Default *int64 `json:"default,omitempty"`
	// Human-readable description.
	// Optional. Omitted and null are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Maximum value (inclusive).
	// Optional. Omitted and null are equivalent and mean there is no inclusive upper bound.
	Maximum *int64 `json:"maximum,omitempty"`
	// Minimum value (inclusive).
	// Optional. Omitted and null are equivalent and mean there is no inclusive lower bound.
	Minimum *int64 `json:"minimum,omitempty"`
	// Optional title for the property.
	// Optional. Omitted and null are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// Request to kill a terminal without releasing it.
type KillTerminalRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The session ID for this request.
	SessionID SessionId `json:"sessionId"`
	// The ID of the terminal to kill.
	TerminalID TerminalId `json:"terminalId"`
}

// Response to terminal/kill method
type KillTerminalResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Request parameters for listing existing sessions.
// Only available if the Agent supports the sessionCapabilities.list capability.
type ListSessionsRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Opaque cursor token from a previous response's nextCursor field for cursor-based pagination
	Cursor *string `json:"cursor,omitempty"`
	// Filter sessions by working directory. Must be an absolute path.
	Cwd *string `json:"cwd,omitempty"`
}

// Response from listing sessions.
type ListSessionsResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Opaque cursor token. If present, pass this in the next request's cursor parameter
	// to fetch the next page. If absent, there are no more results.
	NextCursor *string `json:"nextCursor,omitempty"`
	// Array of session information objects
	Sessions []SessionInfo `json:"sessions"`
}

// Request parameters for loading an existing session.
// Only available if the Agent supports the loadSession capability.
// See protocol docs: Loading Sessions
type LoadSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Additional workspace roots to activate for this session. Each path must be absolute.
	// When omitted or empty, no additional roots are activated. When non-empty,
	// this is the complete resulting additional-root list for the loaded
	// session. It may differ from any previously used or reported list as long as
	// the request cwd matches the session's cwd.
	AdditionalDirectories []string `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd string `json:"cwd"`
	// List of MCP servers to connect to for this session.
	MCPServers []McpServer `json:"mcpServers"`
	// The ID of the session to load.
	SessionID SessionId `json:"sessionId"`
}

// Response from loading an existing session.
type LoadSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Initial session configuration options if supported by the Agent.
	ConfigOptions []SessionConfigOption `json:"configOptions,omitempty"`
	// Initial mode state if supported by the Agent
	// See protocol docs: Session Modes
	Modes *SessionModeState `json:"modes,omitempty"`
}

// Logout capabilities supported by the agent.
// Supplying {} means the agent supports the logout method.
type LogoutCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Request parameters for the logout method.
// Terminates the current authenticated session.
type LogoutRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Response to the logout method.
type LogoutResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// MCP capabilities supported by the agent
type McpCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Agent supports [McpServer::Http].
	HTTP *bool `json:"http,omitempty"`
	// Agent supports [McpServer::Sse].
	SSE *bool `json:"sse,omitempty"`
}

// Configuration for connecting to an MCP (Model Context Protocol) server.
// MCP servers provide tools and context that the agent can use when
// processing prompts.
// See protocol docs: MCP Servers
type McpServerKind string

const (
	McpServerKindHttp McpServerKind = "http"
	McpServerKindSse  McpServerKind = "sse"
)

// McpServer is a tagged union discriminated by "type".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type McpServer struct {
	Kind  McpServerKind   `json:"-"`
	Stdio *McpServerStdio `json:"-"`
	HTTP  *McpServerHttp  `json:"-"`
	SSE   *McpServerSse   `json:"-"`
}

func (v McpServer) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.Stdio != nil {
		set++
	}
	if v.HTTP != nil {
		set++
	}
	if v.SSE != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "McpServer", set)
	}
	if v.Stdio != nil {
		payload, err := json.Marshal(v.Stdio)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("McpServer payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.HTTP != nil {
		if tag, err := json.Marshal("http"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.HTTP)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("McpServer payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.SSE != nil {
		if tag, err := json.Marshal("sse"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.SSE)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("McpServer payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *McpServer) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = McpServer{}
	var tag string
	if raw, ok := raws["type"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("McpServer tag: %w", err)
		}
	}
	switch tag {
	case "":
		v.Stdio = new(McpServerStdio)
		return json.Unmarshal(data, v.Stdio)
	case "http":
		v.Kind = McpServerKindHttp
		v.HTTP = new(McpServerHttp)
		return json.Unmarshal(data, v.HTTP)
	case "sse":
		v.Kind = McpServerKindSse
		v.SSE = new(McpServerSse)
		return json.Unmarshal(data, v.SSE)
	default:
		return fmt.Errorf("McpServer: unknown type tag %q", tag)
	}
}

// HTTP transport configuration for MCP.
type McpServerHttp struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// HTTP headers to set when making requests to the MCP server.
	Headers []HttpHeader `json:"headers"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
	// URL to the MCP server.
	URL string `json:"url"`
}

// SSE transport configuration for MCP.
type McpServerSse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// HTTP headers to set when making requests to the MCP server.
	Headers []HttpHeader `json:"headers"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
	// URL to the MCP server.
	URL string `json:"url"`
}

// Stdio transport configuration for MCP.
type McpServerStdio struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Command-line arguments to pass to the MCP server.
	Args []string `json:"args"`
	// Absolute path to the MCP server executable.
	Command string `json:"command"`
	// Environment variables to set when launching the MCP server.
	Env []EnvVariable `json:"env"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
}

// Unique identifier for a message within a session.
type MessageId string

// Items for a multi-select (array) property schema.
type MultiSelectItemsKind string

const (
	MultiSelectItemsKindString MultiSelectItemsKind = "string"
)

// MultiSelectItems is a tagged union discriminated by "type".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type MultiSelectItems struct {
	Kind   MultiSelectItemsKind    `json:"-"`
	Titled *TitledMultiSelectItems `json:"-"`
	String *StringMultiSelectItems `json:"-"`
	Other  *MultiSelectItemsOther  `json:"-"`
}

// MultiSelectItemsOther preserves an unrecognized MultiSelectItems payload verbatim.
type MultiSelectItemsOther struct{ Raw json.RawMessage }

func (v MultiSelectItemsOther) MarshalJSON() ([]byte, error) { return v.Raw, nil }

func (v MultiSelectItems) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.Titled != nil {
		set++
	}
	if v.String != nil {
		set++
	}
	if v.Other != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "MultiSelectItems", set)
	}
	if v.Titled != nil {
		payload, err := json.Marshal(v.Titled)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("MultiSelectItems payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.String != nil {
		if tag, err := json.Marshal("string"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.String)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("MultiSelectItems payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Other != nil {
		return v.Other.Raw, nil
	}
	return json.Marshal(obj)
}

func (v *MultiSelectItems) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = MultiSelectItems{}
	var tag string
	if raw, ok := raws["type"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("MultiSelectItems tag: %w", err)
		}
	}
	switch tag {
	case "":
		v.Titled = new(TitledMultiSelectItems)
		return json.Unmarshal(data, v.Titled)
	case "string":
		v.Kind = MultiSelectItemsKindString
		v.String = new(StringMultiSelectItems)
		return json.Unmarshal(data, v.String)
	default:
		v.Other = &MultiSelectItemsOther{Raw: append(json.RawMessage(nil), data...)}
		return nil
	}
}

// Schema for multi-select (array) properties in an elicitation form.
type MultiSelectPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Default selected values.
	// Optional. Omitted and null are equivalent and mean no default selections are provided.
	Default []string `json:"default,omitempty"`
	// Human-readable description.
	// Optional. Omitted and null are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// The items definition describing allowed values.
	Items MultiSelectItems `json:"items"`
	// Maximum number of items to select.
	// Optional. Omitted and null are equivalent and mean there is no maximum selection count.
	MaxItems *uint64 `json:"maxItems,omitempty"`
	// Minimum number of items to select.
	// Optional. Omitted and null are equivalent and mean there is no minimum selection count.
	MinItems *uint64 `json:"minItems,omitempty"`
	// Optional title for the property.
	// Optional. Omitted and null are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// Request parameters for creating a new session.
// See protocol docs: Creating a Session
type NewSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Additional workspace roots for this session. Each path must be absolute.
	// These expand the session's filesystem scope without changing cwd, which
	// remains the base for relative paths. When omitted or empty, no
	// additional roots are activated for the new session.
	AdditionalDirectories []string `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd string `json:"cwd"`
	// List of MCP (Model Context Protocol) servers the agent should connect to.
	MCPServers []McpServer `json:"mcpServers"`
}

// Response from creating a new session.
// See protocol docs: Creating a Session
type NewSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Initial session configuration options if supported by the Agent.
	ConfigOptions []SessionConfigOption `json:"configOptions,omitempty"`
	// Initial mode state if supported by the Agent
	// See protocol docs: Session Modes
	Modes *SessionModeState `json:"modes,omitempty"`
	// Unique identifier for the created session.
	// Used in all subsequent requests for this conversation.
	SessionID SessionId `json:"sessionId"`
}

// Schema for number (floating-point) properties in an elicitation form.
type NumberPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Default value.
	// Optional. Omitted and null are equivalent and mean no default value is provided.
	Default *float64 `json:"default,omitempty"`
	// Human-readable description.
	// Optional. Omitted and null are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Maximum value (inclusive).
	// Optional. Omitted and null are equivalent and mean there is no inclusive upper bound.
	Maximum *float64 `json:"maximum,omitempty"`
	// Minimum value (inclusive).
	// Optional. Omitted and null are equivalent and mean there is no inclusive lower bound.
	Minimum *float64 `json:"minimum,omitempty"`
	// Optional title for the property.
	// Optional. Omitted and null are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// An option presented to the user when requesting permission.
type PermissionOption struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Hint about the nature of this permission option.
	Kind PermissionOptionKind `json:"kind"`
	// Human-readable label to display to the user.
	Name string `json:"name"`
	// Unique identifier for this permission option.
	OptionID PermissionOptionId `json:"optionId"`
}

// Unique identifier for a permission option.
type PermissionOptionId string

// The type of permission option being presented to the user.
// Helps clients choose appropriate icons and UI treatment.
type PermissionOptionKind string

const (
	// Allow this operation only this time.
	PermissionOptionKindAllowOnce PermissionOptionKind = "allow_once"
	// Allow this operation and remember the choice.
	PermissionOptionKindAllowAlways PermissionOptionKind = "allow_always"
	// Reject this operation only this time.
	PermissionOptionKindRejectOnce PermissionOptionKind = "reject_once"
	// Reject this operation and remember the choice.
	PermissionOptionKindRejectAlways PermissionOptionKind = "reject_always"
)

// An execution plan for accomplishing complex tasks.
// Plans consist of multiple entries representing individual tasks or goals.
// Agents report plans to clients to provide visibility into their execution strategy.
// Plans can evolve during execution as the agent discovers new requirements or completes tasks.
// See protocol docs: Agent Plan
type Plan struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The list of tasks to be accomplished.
	// When updating a plan, the agent must send a complete list of all entries
	// with their current status. The client replaces the entire plan with each update.
	Entries []PlanEntry `json:"entries"`
}

// A single entry in the execution plan.
// Represents a task or goal that the assistant intends to accomplish
// as part of fulfilling the user's request.
// See protocol docs: Plan Entries
type PlanEntry struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Human-readable description of what this task aims to accomplish.
	Content string `json:"content"`
	// The relative importance of this task.
	// Used to indicate which tasks are most critical to the overall goal.
	Priority PlanEntryPriority `json:"priority"`
	// Current execution status of this task.
	Status PlanEntryStatus `json:"status"`
}

// Priority levels for plan entries.
// Used to indicate the relative importance or urgency of different
// tasks in the execution plan.
// See protocol docs: Plan Entries
type PlanEntryPriority string

const (
	// High priority task - critical to the overall goal.
	PlanEntryPriorityHigh PlanEntryPriority = "high"
	// Medium priority task - important but not critical.
	PlanEntryPriorityMedium PlanEntryPriority = "medium"
	// Low priority task - nice to have but not essential.
	PlanEntryPriorityLow PlanEntryPriority = "low"
)

// Status of a plan entry in the execution flow.
// Tracks the lifecycle of each task from planning through completion.
// See protocol docs: Plan Entries
type PlanEntryStatus string

const (
	// The task has not started yet.
	PlanEntryStatusPending PlanEntryStatus = "pending"
	// The task is currently being worked on.
	PlanEntryStatusInProgress PlanEntryStatus = "in_progress"
	// The task has been successfully completed.
	PlanEntryStatusCompleted PlanEntryStatus = "completed"
)

// Prompt capabilities supported by the agent in session/prompt requests.
// Baseline agent functionality requires support for [ContentBlock::Text]
// and [ContentBlock::ResourceLink] in prompt requests.
// Other variants must be explicitly opted in to.
// Capabilities for different types of content in prompt requests.
// Indicates which content types beyond the baseline (text and resource links)
// the agent can process.
// See protocol docs: Prompt Capabilities
type PromptCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Agent supports [ContentBlock::Audio].
	Audio *bool `json:"audio,omitempty"`
	// Agent supports embedded context in session/prompt requests.
	// When enabled, the Client is allowed to include [ContentBlock::Resource]
	// in prompt requests for pieces of context that are referenced in the message.
	EmbeddedContext *bool `json:"embeddedContext,omitempty"`
	// Agent supports [ContentBlock::Image].
	Image *bool `json:"image,omitempty"`
}

// Request parameters for sending a user prompt to the agent.
// Contains the user's message and any additional context.
// See protocol docs: User Message
type PromptRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The blocks of content that compose the user's message.
	// As a baseline, the Agent MUST support [ContentBlock::Text] and [ContentBlock::ResourceLink],
	// while other variants are optionally enabled via [PromptCapabilities].
	// The Client MUST adapt its interface according to [PromptCapabilities].
	// The client MAY include referenced pieces of context as either
	// [ContentBlock::Resource] or [ContentBlock::ResourceLink].
	// When available, [ContentBlock::Resource] is preferred
	// as it avoids extra round-trips and allows the message to include
	// pieces of context from sources the agent may not have access to.
	Prompt []ContentBlock `json:"prompt"`
	// The ID of the session to send this user message to
	SessionID SessionId `json:"sessionId"`
}

// Response from processing a user prompt.
// See protocol docs: Check for Completion
type PromptResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Indicates why the agent stopped processing the turn.
	StopReason StopReason `json:"stopReason"`
}

// Protocol version identifier.
// This version is only bumped for breaking changes.
// Non-breaking changes should be introduced via capabilities.
type ProtocolVersion int64

// Request to read content from a text file.
// Only available if the client supports the fs.readTextFile capability.
type ReadTextFileRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Maximum number of lines to read.
	Limit *int64 `json:"limit,omitempty"`
	// Line number to start reading from (1-based).
	Line *int64 `json:"line,omitempty"`
	// Absolute path to the file to read.
	Path string `json:"path"`
	// The session ID for this request.
	SessionID SessionId `json:"sessionId"`
}

// Response containing the contents of a text file.
type ReadTextFileResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Content payload returned by this response.
	Content string `json:"content"`
}

// Request to release a terminal and free its resources.
type ReleaseTerminalRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The session ID for this request.
	SessionID SessionId `json:"sessionId"`
	// The ID of the terminal to release.
	TerminalID TerminalId `json:"terminalId"`
}

// Response to terminal/release method
type ReleaseTerminalResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// JSON RPC Request Id
// An identifier established by the Client that MUST contain a String, Number, or NULL value if included. If it is not included it is assumed to be a notification. The value SHOULD normally not be Null \[1\] and Numbers SHOULD NOT contain fractional parts \[2\]
// The Server MUST reply with the same value in the Response object if included. This member is used to correlate the context between the two objects.
// \[1\] The use of Null as a value for the id member in a Request object is discouraged, because this specification uses a value of Null for Responses with an unknown id. Also, because JSON-RPC 1.0 uses an id value of Null for Notifications this could cause confusion in handling.
// \[2\] Fractional parts may be problematic, since many decimal fractions cannot be represented exactly as binary fractions.
type RequestId any

// The outcome of a permission request.
type RequestPermissionOutcomeKind string

const (
	RequestPermissionOutcomeKindCancelled RequestPermissionOutcomeKind = "cancelled"
	RequestPermissionOutcomeKindSelected  RequestPermissionOutcomeKind = "selected"
)

// RequestPermissionOutcome is a tagged union discriminated by "outcome".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type RequestPermissionOutcome struct {
	Kind      RequestPermissionOutcomeKind       `json:"-"`
	Cancelled *RequestPermissionOutcomeCancelled `json:"-"`
	Selected  *SelectedPermissionOutcome         `json:"-"`
}

type RequestPermissionOutcomeCancelled struct {
}

func (v RequestPermissionOutcome) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.Cancelled != nil {
		set++
	}
	if v.Selected != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "RequestPermissionOutcome", set)
	}
	if v.Cancelled != nil {
		if tag, err := json.Marshal("cancelled"); err != nil {
			return nil, err
		} else {
			obj["outcome"] = tag
		}
		payload, err := json.Marshal(v.Cancelled)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("RequestPermissionOutcome payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Selected != nil {
		if tag, err := json.Marshal("selected"); err != nil {
			return nil, err
		} else {
			obj["outcome"] = tag
		}
		payload, err := json.Marshal(v.Selected)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("RequestPermissionOutcome payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *RequestPermissionOutcome) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = RequestPermissionOutcome{}
	var tag string
	if raw, ok := raws["outcome"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("RequestPermissionOutcome tag: %w", err)
		}
	}
	switch tag {
	case "cancelled":
		v.Kind = RequestPermissionOutcomeKindCancelled
		v.Cancelled = new(RequestPermissionOutcomeCancelled)
		return json.Unmarshal(data, v.Cancelled)
	case "selected":
		v.Kind = RequestPermissionOutcomeKindSelected
		v.Selected = new(SelectedPermissionOutcome)
		return json.Unmarshal(data, v.Selected)
	default:
		return fmt.Errorf("RequestPermissionOutcome: unknown outcome tag %q", tag)
	}
}

// Request for user permission to execute a tool call.
// Sent when the agent needs authorization before performing a sensitive operation.
// See protocol docs: Requesting Permission
type RequestPermissionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Available permission options for the user to choose from.
	Options []PermissionOption `json:"options"`
	// The session ID for this request.
	SessionID SessionId `json:"sessionId"`
	// Details about the tool call requiring permission.
	ToolCall ToolCallUpdate `json:"toolCall"`
}

// Response to a permission request.
type RequestPermissionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The user's decision on the permission request.
	Outcome RequestPermissionOutcome `json:"outcome"`
}

// A resource that the server is capable of reading, included in a prompt or tool call result.
type ResourceLink struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Optional human-readable details shown with this protocol object.
	Description *string `json:"description,omitempty"`
	// MIME type describing the encoded media payload.
	MimeType *string `json:"mimeType,omitempty"`
	// Human-readable name shown for this protocol object.
	Name string `json:"name"`
	// Optional size of the linked resource in bytes, if known.
	Size *int64 `json:"size,omitempty"`
	// Optional display title for end-user UI.
	Title *string `json:"title,omitempty"`
	// URI associated with this resource or media payload.
	URI string `json:"uri"`
}

// Request parameters for resuming an existing session.
// Resumes an existing session without returning previous messages (unlike session/load).
// This is useful for agents that can resume sessions but don't implement full session loading.
// Only available if the Agent supports the sessionCapabilities.resume capability.
type ResumeSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Additional workspace roots to activate for this session. Each path must be absolute.
	// When omitted or empty, no additional roots are activated. When non-empty,
	// this is the complete resulting additional-root list for the resumed
	// session. It may differ from any previously used or reported list as long as
	// the request cwd matches the session's cwd.
	AdditionalDirectories []string `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd string `json:"cwd"`
	// List of MCP servers to connect to for this session.
	MCPServers []McpServer `json:"mcpServers,omitempty"`
	// The ID of the session to resume.
	SessionID SessionId `json:"sessionId"`
}

// Response from resuming an existing session.
type ResumeSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Initial session configuration options if supported by the Agent.
	ConfigOptions []SessionConfigOption `json:"configOptions,omitempty"`
	// Initial mode state if supported by the Agent
	// See protocol docs: Session Modes
	Modes *SessionModeState `json:"modes,omitempty"`
}

// The sender or recipient of messages and data in a conversation.
type Role string

const (
	// The assistant side of a conversation.
	RoleAssistant Role = "assistant"
	// The user side of a conversation.
	RoleUser Role = "user"
)

// The user selected one of the provided options.
type SelectedPermissionOutcome struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the option the user selected.
	OptionID PermissionOptionId `json:"optionId"`
}

// Capabilities for additional session directories support.
// Supplying {} means the agent supports the additionalDirectories field on
// supported session lifecycle requests. Agents that also support
// session/list may return SessionInfo.additionalDirectories to report the
// complete ordered additional-root list associated with a listed session.
type SessionAdditionalDirectoriesCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Session capabilities supported by the agent.
// As a baseline, all Agents MUST support session/new, session/prompt, session/cancel, and session/update.
// Optionally, they MAY support other session methods and notifications by specifying additional capabilities.
// Note: session/load is still handled by the top-level load_session capability. This will be unified in future versions of the protocol.
// See protocol docs: Session Capabilities
type SessionCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Whether the agent supports additionalDirectories on supported session lifecycle requests.
	// Optional. Omitted or null both mean the agent does not advertise support.
	// Supplying {} means the agent supports additionalDirectories on
	// supported session lifecycle requests.
	// Agents that also support session/list may return
	// SessionInfo.additionalDirectories to report the complete ordered
	// additional-root list associated with a listed session.
	AdditionalDirectories *SessionAdditionalDirectoriesCapabilities `json:"additionalDirectories,omitempty"`
	// Whether the agent supports session/close.
	// Optional. Omitted or null both mean the agent does not advertise support.
	// Supplying {} means the agent supports closing sessions.
	Close *SessionCloseCapabilities `json:"close,omitempty"`
	// Whether the agent supports session/delete.
	// Optional. Omitted or null both mean the agent does not advertise support.
	// Supplying {} means the agent supports deleting sessions from session/list.
	Delete *SessionDeleteCapabilities `json:"delete,omitempty"`
	// Whether the agent supports session/list.
	// Optional. Omitted or null both mean the agent does not advertise support.
	// Supplying {} means the agent supports listing sessions.
	List *SessionListCapabilities `json:"list,omitempty"`
	// Whether the agent supports session/resume.
	// Optional. Omitted or null both mean the agent does not advertise support.
	// Supplying {} means the agent supports resuming sessions.
	Resume *SessionResumeCapabilities `json:"resume,omitempty"`
}

// Capabilities for the session/close method.
// Supplying {} means the agent supports closing sessions.
type SessionCloseCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// A boolean on/off toggle session configuration option payload.
type SessionConfigBoolean struct {
	// The current value of the boolean option.
	CurrentValue bool `json:"currentValue"`
}

// Unique identifier for a session configuration option value group.
type SessionConfigGroupId string

// Unique identifier for a session configuration option.
type SessionConfigId string

// A session configuration option selector and its current state.
type SessionConfigOptionKind string

const (
	SessionConfigOptionKindSelect  SessionConfigOptionKind = "select"
	SessionConfigOptionKindBoolean SessionConfigOptionKind = "boolean"
)

// SessionConfigOption is a tagged union discriminated by "type".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type SessionConfigOption struct {
	Kind SessionConfigOptionKind `json:"-"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional semantic category for this option (UX only).
	Category *SessionConfigOptionCategory `json:"category,omitempty"`
	// Optional description for the Client to display to the user.
	Description *string `json:"description,omitempty"`
	// Unique identifier for the configuration option.
	ID SessionConfigId `json:"id"`
	// Human-readable label for the option.
	Name    string                `json:"name"`
	Select  *SessionConfigSelect  `json:"-"`
	Boolean *SessionConfigBoolean `json:"-"`
}

// sessionConfigOptionShadow mirrors SessionConfigOption without its union methods, for encoding common fields.
type sessionConfigOptionShadow SessionConfigOption

func (v SessionConfigOption) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	if common, err := json.Marshal(sessionConfigOptionShadow(v)); err != nil {
		return nil, err
	} else if len(common) > 2 {
		if err := json.Unmarshal(common, &obj); err != nil {
			return nil, err
		}
	}
	set := 0
	if v.Select != nil {
		set++
	}
	if v.Boolean != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "SessionConfigOption", set)
	}
	if v.Select != nil {
		if tag, err := json.Marshal("select"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Select)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionConfigOption payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Boolean != nil {
		if tag, err := json.Marshal("boolean"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Boolean)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionConfigOption payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *SessionConfigOption) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = SessionConfigOption{}
	if err := json.Unmarshal(data, (*sessionConfigOptionShadow)(v)); err != nil {
		return err
	}
	var tag string
	if raw, ok := raws["type"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("SessionConfigOption tag: %w", err)
		}
	}
	switch tag {
	case "select":
		v.Kind = SessionConfigOptionKindSelect
		v.Select = new(SessionConfigSelect)
		return json.Unmarshal(data, v.Select)
	case "boolean":
		v.Kind = SessionConfigOptionKindBoolean
		v.Boolean = new(SessionConfigBoolean)
		return json.Unmarshal(data, v.Boolean)
	default:
		return fmt.Errorf("SessionConfigOption: unknown type tag %q", tag)
	}
}

// Semantic category for a session configuration option.
// This is intended to help Clients distinguish broadly common selectors (e.g. model selector vs
// session mode selector vs thought/reasoning level) for UX purposes (keyboard shortcuts, icons,
// placement). It MUST NOT be required for correctness. Clients MUST handle missing or unknown
// categories gracefully.
// Category names beginning with _ are free for custom use, like other ACP extension methods.
// Category names that do not begin with _ are reserved for the ACP spec.
type SessionConfigOptionCategory string

const (
	// Session mode selector.
	SessionConfigOptionCategoryMode SessionConfigOptionCategory = "mode"
	// Model selector.
	SessionConfigOptionCategoryModel SessionConfigOptionCategory = "model"
	// Model-related configuration parameter.
	SessionConfigOptionCategoryModelConfig SessionConfigOptionCategory = "model_config"
	// Thought/reasoning level selector.
	SessionConfigOptionCategoryThoughtLevel SessionConfigOptionCategory = "thought_level"
)

// Session configuration option capabilities supported by the client.
type SessionConfigOptionsCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Whether the client supports boolean session configuration options.
	// Optional. Omitted or null both mean the client does not advertise support.
	// Supplying {} means agents may include type: "boolean" entries in
	// configOptions, and the client may send session/set_config_option
	// requests with type: "boolean" and a boolean value.
	Boolean *BooleanConfigOptionCapabilities `json:"boolean,omitempty"`
}

// A single-value selector (dropdown) session configuration option payload.
type SessionConfigSelect struct {
	// The currently selected value.
	CurrentValue SessionConfigValueId `json:"currentValue"`
	// The set of selectable options.
	Options SessionConfigSelectOptions `json:"options"`
}

// A group of possible values for a session configuration option.
type SessionConfigSelectGroup struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Unique identifier for this group.
	Group SessionConfigGroupId `json:"group"`
	// Human-readable label for this group.
	Name string `json:"name"`
	// The set of option values in this group.
	Options []SessionConfigSelectOption `json:"options"`
}

// A possible value for a session configuration option.
type SessionConfigSelectOption struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional description for this option value.
	Description *string `json:"description,omitempty"`
	// Human-readable label for this option value.
	Name string `json:"name"`
	// Unique identifier for this option value.
	Value SessionConfigValueId `json:"value"`
}

// Possible values for a session configuration option.
type SessionConfigSelectOptions any

// Unique identifier for a session configuration option value.
type SessionConfigValueId string

// Capabilities for the session/delete method.
// Supplying {} means the agent supports deleting sessions from session/list.
type SessionDeleteCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// A unique identifier for a conversation session between a client and agent.
// Sessions maintain their own context, conversation history, and state,
// allowing multiple independent interactions with the same agent.
// See protocol docs: Session ID
type SessionId string

// Information about a session returned by session/list
type SessionInfo struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Additional workspace roots reported for this session. Each path must be absolute.
	// When present, this is the complete ordered additional-root list reported
	// by the Agent. Omitted and empty values are equivalent: the response
	// reports no additional roots.
	AdditionalDirectories []string `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd string `json:"cwd"`
	// Unique identifier for the session
	SessionID SessionId `json:"sessionId"`
	// Human-readable title for the session
	Title *string `json:"title,omitempty"`
	// ISO 8601 timestamp of last activity
	UpdatedAt *string `json:"updatedAt,omitempty"`
}

// Update to session metadata. All fields are optional to support partial updates.
// Agents send this notification to update session information like title or custom metadata.
// This allows clients to display dynamic session names and track session state changes.
type SessionInfoUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Human-readable title for the session. Set to null to clear.
	Title *string `json:"title,omitempty"`
	// ISO 8601 timestamp of last activity. Set to null to clear.
	UpdatedAt *string `json:"updatedAt,omitempty"`
}

// Capabilities for the session/list method.
// Supplying {} means the agent supports listing sessions.
type SessionListCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// A mode the agent can operate in.
// See protocol docs: Session Modes
type SessionMode struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional human-readable details shown with this protocol object.
	Description *string `json:"description,omitempty"`
	// Stable identifier used to refer to this protocol object in later messages.
	ID SessionModeId `json:"id"`
	// Human-readable name shown for this protocol object.
	Name string `json:"name"`
}

// Unique identifier for a Session Mode.
type SessionModeId string

// The set of modes and the one currently active.
type SessionModeState struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The set of modes that the Agent can operate in
	AvailableModes []SessionMode `json:"availableModes"`
	// The current mode the Agent is in.
	CurrentModeID SessionModeId `json:"currentModeId"`
}

// Notification containing a session update from the agent.
// Used to stream real-time progress and results during prompt processing.
// See protocol docs: Agent Reports Output
type SessionNotification struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the session this update pertains to.
	SessionID SessionId `json:"sessionId"`
	// The actual update content.
	Update SessionUpdate `json:"update"`
}

// Capabilities for the session/resume method.
// Supplying {} means the agent supports resuming sessions.
type SessionResumeCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Different types of updates that can be sent during session processing.
// These updates provide real-time feedback about the agent's progress.
// See protocol docs: Agent Reports Output
type SessionUpdateKind string

const (
	SessionUpdateKindUserMessageChunk        SessionUpdateKind = "user_message_chunk"
	SessionUpdateKindAgentMessageChunk       SessionUpdateKind = "agent_message_chunk"
	SessionUpdateKindAgentThoughtChunk       SessionUpdateKind = "agent_thought_chunk"
	SessionUpdateKindToolCall                SessionUpdateKind = "tool_call"
	SessionUpdateKindToolCallUpdate          SessionUpdateKind = "tool_call_update"
	SessionUpdateKindPlan                    SessionUpdateKind = "plan"
	SessionUpdateKindAvailableCommandsUpdate SessionUpdateKind = "available_commands_update"
	SessionUpdateKindCurrentModeUpdate       SessionUpdateKind = "current_mode_update"
	SessionUpdateKindConfigOptionUpdate      SessionUpdateKind = "config_option_update"
	SessionUpdateKindSessionInfoUpdate       SessionUpdateKind = "session_info_update"
	SessionUpdateKindUsageUpdate             SessionUpdateKind = "usage_update"
)

// SessionUpdate is a tagged union discriminated by "sessionUpdate".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type SessionUpdate struct {
	Kind                    SessionUpdateKind        `json:"-"`
	UserMessageChunk        *ContentChunk            `json:"-"`
	AgentMessageChunk       *ContentChunk            `json:"-"`
	AgentThoughtChunk       *ContentChunk            `json:"-"`
	ToolCall                *ToolCall                `json:"-"`
	ToolCallUpdate          *ToolCallUpdate          `json:"-"`
	Plan                    *Plan                    `json:"-"`
	AvailableCommandsUpdate *AvailableCommandsUpdate `json:"-"`
	CurrentModeUpdate       *CurrentModeUpdate       `json:"-"`
	ConfigOptionUpdate      *ConfigOptionUpdate      `json:"-"`
	SessionInfoUpdate       *SessionInfoUpdate       `json:"-"`
	UsageUpdate             *UsageUpdate             `json:"-"`
}

func (v SessionUpdate) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.UserMessageChunk != nil {
		set++
	}
	if v.AgentMessageChunk != nil {
		set++
	}
	if v.AgentThoughtChunk != nil {
		set++
	}
	if v.ToolCall != nil {
		set++
	}
	if v.ToolCallUpdate != nil {
		set++
	}
	if v.Plan != nil {
		set++
	}
	if v.AvailableCommandsUpdate != nil {
		set++
	}
	if v.CurrentModeUpdate != nil {
		set++
	}
	if v.ConfigOptionUpdate != nil {
		set++
	}
	if v.SessionInfoUpdate != nil {
		set++
	}
	if v.UsageUpdate != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "SessionUpdate", set)
	}
	if v.UserMessageChunk != nil {
		if tag, err := json.Marshal("user_message_chunk"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.UserMessageChunk)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.AgentMessageChunk != nil {
		if tag, err := json.Marshal("agent_message_chunk"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.AgentMessageChunk)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.AgentThoughtChunk != nil {
		if tag, err := json.Marshal("agent_thought_chunk"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.AgentThoughtChunk)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.ToolCall != nil {
		if tag, err := json.Marshal("tool_call"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.ToolCall)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.ToolCallUpdate != nil {
		if tag, err := json.Marshal("tool_call_update"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.ToolCallUpdate)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Plan != nil {
		if tag, err := json.Marshal("plan"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.Plan)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.AvailableCommandsUpdate != nil {
		if tag, err := json.Marshal("available_commands_update"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.AvailableCommandsUpdate)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.CurrentModeUpdate != nil {
		if tag, err := json.Marshal("current_mode_update"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.CurrentModeUpdate)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.ConfigOptionUpdate != nil {
		if tag, err := json.Marshal("config_option_update"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.ConfigOptionUpdate)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.SessionInfoUpdate != nil {
		if tag, err := json.Marshal("session_info_update"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.SessionInfoUpdate)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.UsageUpdate != nil {
		if tag, err := json.Marshal("usage_update"); err != nil {
			return nil, err
		} else {
			obj["sessionUpdate"] = tag
		}
		payload, err := json.Marshal(v.UsageUpdate)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SessionUpdate payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *SessionUpdate) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = SessionUpdate{}
	var tag string
	if raw, ok := raws["sessionUpdate"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("SessionUpdate tag: %w", err)
		}
	}
	switch tag {
	case "user_message_chunk":
		v.Kind = SessionUpdateKindUserMessageChunk
		v.UserMessageChunk = new(ContentChunk)
		return json.Unmarshal(data, v.UserMessageChunk)
	case "agent_message_chunk":
		v.Kind = SessionUpdateKindAgentMessageChunk
		v.AgentMessageChunk = new(ContentChunk)
		return json.Unmarshal(data, v.AgentMessageChunk)
	case "agent_thought_chunk":
		v.Kind = SessionUpdateKindAgentThoughtChunk
		v.AgentThoughtChunk = new(ContentChunk)
		return json.Unmarshal(data, v.AgentThoughtChunk)
	case "tool_call":
		v.Kind = SessionUpdateKindToolCall
		v.ToolCall = new(ToolCall)
		return json.Unmarshal(data, v.ToolCall)
	case "tool_call_update":
		v.Kind = SessionUpdateKindToolCallUpdate
		v.ToolCallUpdate = new(ToolCallUpdate)
		return json.Unmarshal(data, v.ToolCallUpdate)
	case "plan":
		v.Kind = SessionUpdateKindPlan
		v.Plan = new(Plan)
		return json.Unmarshal(data, v.Plan)
	case "available_commands_update":
		v.Kind = SessionUpdateKindAvailableCommandsUpdate
		v.AvailableCommandsUpdate = new(AvailableCommandsUpdate)
		return json.Unmarshal(data, v.AvailableCommandsUpdate)
	case "current_mode_update":
		v.Kind = SessionUpdateKindCurrentModeUpdate
		v.CurrentModeUpdate = new(CurrentModeUpdate)
		return json.Unmarshal(data, v.CurrentModeUpdate)
	case "config_option_update":
		v.Kind = SessionUpdateKindConfigOptionUpdate
		v.ConfigOptionUpdate = new(ConfigOptionUpdate)
		return json.Unmarshal(data, v.ConfigOptionUpdate)
	case "session_info_update":
		v.Kind = SessionUpdateKindSessionInfoUpdate
		v.SessionInfoUpdate = new(SessionInfoUpdate)
		return json.Unmarshal(data, v.SessionInfoUpdate)
	case "usage_update":
		v.Kind = SessionUpdateKindUsageUpdate
		v.UsageUpdate = new(UsageUpdate)
		return json.Unmarshal(data, v.UsageUpdate)
	default:
		return fmt.Errorf("SessionUpdate: unknown sessionUpdate tag %q", tag)
	}
}

// Request parameters for setting a session configuration option.
type SetSessionConfigOptionRequestKind string

const (
	SetSessionConfigOptionRequestKindBoolean SetSessionConfigOptionRequestKind = "boolean"
)

// SetSessionConfigOptionRequest is a tagged union discriminated by "type".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type SetSessionConfigOptionRequest struct {
	Kind SetSessionConfigOptionRequestKind `json:"-"`
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the configuration option to set.
	ConfigID SessionConfigId `json:"configId"`
	// The ID of the session to set the configuration option for.
	SessionID SessionId                             `json:"sessionId"`
	ValueID   *SetSessionConfigOptionRequestValueID `json:"-"`
	Boolean   *SetSessionConfigOptionRequestBoolean `json:"-"`
}

type SetSessionConfigOptionRequestValueID struct {
	// The value ID.
	Value SessionConfigValueId `json:"value"`
}

type SetSessionConfigOptionRequestBoolean struct {
	// The boolean value.
	Value bool `json:"value"`
}

// setSessionConfigOptionRequestShadow mirrors SetSessionConfigOptionRequest without its union methods, for encoding common fields.
type setSessionConfigOptionRequestShadow SetSessionConfigOptionRequest

func (v SetSessionConfigOptionRequest) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	if common, err := json.Marshal(setSessionConfigOptionRequestShadow(v)); err != nil {
		return nil, err
	} else if len(common) > 2 {
		if err := json.Unmarshal(common, &obj); err != nil {
			return nil, err
		}
	}
	set := 0
	if v.ValueID != nil {
		set++
	}
	if v.Boolean != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "SetSessionConfigOptionRequest", set)
	}
	if v.ValueID != nil {
		payload, err := json.Marshal(v.ValueID)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SetSessionConfigOptionRequest payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Boolean != nil {
		if tag, err := json.Marshal("boolean"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Boolean)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("SetSessionConfigOptionRequest payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *SetSessionConfigOptionRequest) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = SetSessionConfigOptionRequest{}
	if err := json.Unmarshal(data, (*setSessionConfigOptionRequestShadow)(v)); err != nil {
		return err
	}
	var tag string
	if raw, ok := raws["type"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("SetSessionConfigOptionRequest tag: %w", err)
		}
	}
	switch tag {
	case "":
		v.ValueID = new(SetSessionConfigOptionRequestValueID)
		return json.Unmarshal(data, v.ValueID)
	case "boolean":
		v.Kind = SetSessionConfigOptionRequestKindBoolean
		v.Boolean = new(SetSessionConfigOptionRequestBoolean)
		return json.Unmarshal(data, v.Boolean)
	default:
		return fmt.Errorf("SetSessionConfigOptionRequest: unknown type tag %q", tag)
	}
}

// Response to session/set_config_option method.
type SetSessionConfigOptionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The full set of configuration options and their current values.
	ConfigOptions []SessionConfigOption `json:"configOptions"`
}

// Request parameters for setting a session mode.
type SetSessionModeRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The ID of the mode to set.
	ModeID SessionModeId `json:"modeId"`
	// The ID of the session to set the mode for.
	SessionID SessionId `json:"sessionId"`
}

// Response to session/set_mode method.
type SetSessionModeResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}

// Reasons why an agent stops processing a prompt turn.
// See protocol docs: Stop Reasons
type StopReason string

const (
	// The turn ended successfully.
	StopReasonEndTurn StopReason = "end_turn"
	// The turn ended because the agent reached the maximum number of tokens.
	StopReasonMaxTokens StopReason = "max_tokens"
	// The turn ended because the agent reached the maximum number of allowed
	// agent requests between user turns.
	StopReasonMaxTurnRequests StopReason = "max_turn_requests"
	// The turn ended because the agent refused to continue. The user prompt
	// and everything that comes after it won't be included in the next
	// prompt, so this should be reflected in the UI.
	StopReasonRefusal StopReason = "refusal"
	// The turn was cancelled by the client via session/cancel.
	// This stop reason MUST be returned when the client sends a session/cancel
	// notification, even if the cancellation causes exceptions in underlying operations.
	// Agents should catch these exceptions and return this semantically meaningful
	// response to confirm successful cancellation.
	StopReasonCancelled StopReason = "cancelled"
)

// String format types for string properties in elicitation schemas.
type StringFormat string

const (
	// Email address format.
	StringFormatEmail StringFormat = "email"
	// URI format.
	StringFormatUri StringFormat = "uri"
	// Date format (YYYY-MM-DD).
	StringFormatDate StringFormat = "date"
	// Date-time format (ISO 8601).
	StringFormatDateTime StringFormat = "date-time"
)

// String item schema for multi-select enum properties.
type StringMultiSelectItems struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Allowed enum values.
	Enum []string `json:"enum"`
}

// Schema for string properties in an elicitation form.
// When enum or oneOf is set, this represents a single-select enum
// with "type": "string".
type StringPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Default value.
	// Optional. Omitted and null are equivalent and mean no default value is provided.
	Default *string `json:"default,omitempty"`
	// Human-readable description.
	// Optional. Omitted and null are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Enum values for untitled single-select enums.
	// Optional. Omitted and null are equivalent and mean no untitled single-select choices are
	// declared by enum.
	Enum []string `json:"enum,omitempty"`
	// String format.
	// Optional. Omitted and null are equivalent and mean there is no format constraint.
	Format *StringFormat `json:"format,omitempty"`
	// Maximum string length.
	// Optional. Omitted and null are equivalent and mean there is no maximum length constraint.
	MaxLength *int64 `json:"maxLength,omitempty"`
	// Minimum string length.
	// Optional. Omitted and null are equivalent and mean there is no minimum length constraint.
	MinLength *int64 `json:"minLength,omitempty"`
	// Titled enum options for titled single-select enums.
	// Optional. Omitted and null are equivalent and mean no titled single-select choices are
	// declared by oneOf.
	OneOf []EnumOption `json:"oneOf,omitempty"`
	// Pattern the string must match.
	// Optional. Omitted and null are equivalent and mean there is no pattern constraint.
	Pattern *string `json:"pattern,omitempty"`
	// Optional title for the property.
	// Optional. Omitted and null are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// Embed a terminal created with terminal/create by its id.
// The terminal must be added before calling terminal/release.
// See protocol docs: Terminal
type Terminal struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Identifier of the terminal instance to embed in the content stream.
	TerminalID TerminalId `json:"terminalId"`
}

// Exit status of a terminal command.
type TerminalExitStatus struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The process exit code (may be null if terminated by signal).
	ExitCode *int64 `json:"exitCode,omitempty"`
	// The signal that terminated the process (may be null if exited normally).
	Signal *string `json:"signal,omitempty"`
}

// Typed identifier used for terminal values on the wire.
type TerminalId string

// Request to get the current output and status of a terminal.
type TerminalOutputRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The session ID for this request.
	SessionID SessionId `json:"sessionId"`
	// The ID of the terminal to get output from.
	TerminalID TerminalId `json:"terminalId"`
}

// Response containing the terminal output and exit status.
type TerminalOutputResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Exit status if the command has completed.
	ExitStatus *TerminalExitStatus `json:"exitStatus,omitempty"`
	// The terminal output captured so far.
	Output string `json:"output"`
	// Whether the output was truncated due to byte limits.
	Truncated bool `json:"truncated"`
}

// Text provided to or from an LLM.
type TextContent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Text payload carried by this content block.
	Text string `json:"text"`
}

// Text-based resource contents.
type TextResourceContents struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// MIME type describing the encoded media payload.
	MimeType *string `json:"mimeType,omitempty"`
	// Text payload carried by this content block.
	Text string `json:"text"`
	// URI associated with this resource or media payload.
	URI string `json:"uri"`
}

// Items definition for titled multi-select enum properties.
type TitledMultiSelectItems struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// Optional. Omitted and null are equivalent and mean no metadata.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Titled enum options.
	AnyOf []EnumOption `json:"anyOf"`
}

// Represents a tool call that the language model has requested.
// Tool calls are actions that the agent executes on behalf of the language model,
// such as reading files, executing code, or fetching data from external sources.
// See protocol docs: Tool Calls
type ToolCall struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Content produced by the tool call.
	Content []ToolCallContent `json:"content,omitempty"`
	// The category of tool being invoked.
	// Helps clients choose appropriate icons and UI treatment.
	Kind *ToolKind `json:"kind,omitempty"`
	// File locations affected by this tool call.
	// Enables "follow-along" features in clients.
	Locations []ToolCallLocation `json:"locations,omitempty"`
	// Raw input parameters sent to the tool.
	RawInput json.RawMessage `json:"rawInput,omitempty"`
	// Raw output returned by the tool.
	RawOutput json.RawMessage `json:"rawOutput,omitempty"`
	// Current execution status of the tool call.
	Status *ToolCallStatus `json:"status,omitempty"`
	// Human-readable title describing what the tool is doing.
	Title string `json:"title"`
	// Unique identifier for this tool call within the session.
	ToolCallID ToolCallId `json:"toolCallId"`
}

// Content produced by a tool call.
// Tool calls can produce different types of content including
// standard content blocks (text, images) or file diffs.
// See protocol docs: Content
type ToolCallContentKind string

const (
	ToolCallContentKindContent  ToolCallContentKind = "content"
	ToolCallContentKindDiff     ToolCallContentKind = "diff"
	ToolCallContentKindTerminal ToolCallContentKind = "terminal"
)

// ToolCallContent is a tagged union discriminated by "type".
// Exactly one variant pointer must be set. Kind is set on decode and
// derived from the payload on encode; setting it by hand has no effect.
type ToolCallContent struct {
	Kind     ToolCallContentKind `json:"-"`
	Content  *Content            `json:"-"`
	Diff     *Diff               `json:"-"`
	Terminal *Terminal           `json:"-"`
}

func (v ToolCallContent) MarshalJSON() ([]byte, error) {
	obj := map[string]json.RawMessage{}
	set := 0
	if v.Content != nil {
		set++
	}
	if v.Diff != nil {
		set++
	}
	if v.Terminal != nil {
		set++
	}
	if set != 1 {
		return nil, fmt.Errorf("%s: exactly one variant must be set, have %d", "ToolCallContent", set)
	}
	if v.Content != nil {
		if tag, err := json.Marshal("content"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Content)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ToolCallContent payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Diff != nil {
		if tag, err := json.Marshal("diff"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Diff)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ToolCallContent payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	if v.Terminal != nil {
		if tag, err := json.Marshal("terminal"); err != nil {
			return nil, err
		} else {
			obj["type"] = tag
		}
		payload, err := json.Marshal(v.Terminal)
		if err != nil {
			return nil, err
		}
		var fields map[string]json.RawMessage
		if err := json.Unmarshal(payload, &fields); err != nil {
			return nil, fmt.Errorf("ToolCallContent payload: %w", err)
		}
		for k, raw := range fields {
			obj[k] = raw
		}
	}
	return json.Marshal(obj)
}

func (v *ToolCallContent) UnmarshalJSON(data []byte) error {
	var raws map[string]json.RawMessage
	if err := json.Unmarshal(data, &raws); err != nil {
		return err
	}
	*v = ToolCallContent{}
	var tag string
	if raw, ok := raws["type"]; ok {
		if err := json.Unmarshal(raw, &tag); err != nil {
			return fmt.Errorf("ToolCallContent tag: %w", err)
		}
	}
	switch tag {
	case "content":
		v.Kind = ToolCallContentKindContent
		v.Content = new(Content)
		return json.Unmarshal(data, v.Content)
	case "diff":
		v.Kind = ToolCallContentKindDiff
		v.Diff = new(Diff)
		return json.Unmarshal(data, v.Diff)
	case "terminal":
		v.Kind = ToolCallContentKindTerminal
		v.Terminal = new(Terminal)
		return json.Unmarshal(data, v.Terminal)
	default:
		return fmt.Errorf("ToolCallContent: unknown type tag %q", tag)
	}
}

// Unique identifier for a tool call within a session.
type ToolCallId string

// A file location being accessed or modified by a tool.
// Enables clients to implement "follow-along" features that track
// which files the agent is working with in real-time.
// See protocol docs: Following the Agent
type ToolCallLocation struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Optional line number within the file.
	Line *int64 `json:"line,omitempty"`
	// The absolute file path being accessed or modified.
	Path string `json:"path"`
}

// Execution status of a tool call.
// Tool calls progress through different statuses during their lifecycle.
// See protocol docs: Status
type ToolCallStatus string

const (
	// The tool call hasn't started running yet because the input is either
	// streaming or we're awaiting approval.
	ToolCallStatusPending ToolCallStatus = "pending"
	// The tool call is currently running.
	ToolCallStatusInProgress ToolCallStatus = "in_progress"
	// The tool call completed successfully.
	ToolCallStatusCompleted ToolCallStatus = "completed"
	// The tool call failed with an error.
	ToolCallStatusFailed ToolCallStatus = "failed"
)

// An update to an existing tool call.
// Used to report progress and results as tools execute. All fields except
// the tool call ID are optional - only changed fields need to be included.
// See protocol docs: Updating
type ToolCallUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Replace the content collection.
	Content []ToolCallContent `json:"content,omitempty"`
	// Update the tool kind.
	Kind *ToolKind `json:"kind,omitempty"`
	// Replace the locations collection.
	Locations []ToolCallLocation `json:"locations,omitempty"`
	// Update the raw input.
	RawInput json.RawMessage `json:"rawInput,omitempty"`
	// Update the raw output.
	RawOutput json.RawMessage `json:"rawOutput,omitempty"`
	// Update the execution status.
	Status *ToolCallStatus `json:"status,omitempty"`
	// Update the human-readable title.
	Title *string `json:"title,omitempty"`
	// The ID of the tool call being updated.
	ToolCallID ToolCallId `json:"toolCallId"`
}

// Categories of tools that can be invoked.
// Tool kinds help clients choose appropriate icons and optimize how they
// display tool execution progress.
// See protocol docs: Creating
type ToolKind string

const (
	// Reading files or data.
	ToolKindRead ToolKind = "read"
	// Modifying files or content.
	ToolKindEdit ToolKind = "edit"
	// Removing files or data.
	ToolKindDelete ToolKind = "delete"
	// Moving or renaming files.
	ToolKindMove ToolKind = "move"
	// Searching for information.
	ToolKindSearch ToolKind = "search"
	// Running commands or code.
	ToolKindExecute ToolKind = "execute"
	// Internal reasoning or planning.
	ToolKindThink ToolKind = "think"
	// Retrieving external data.
	ToolKindFetch ToolKind = "fetch"
	// Switching the current session mode.
	ToolKindSwitchMode ToolKind = "switch_mode"
	// Other tool types (default).
	ToolKindOther ToolKind = "other"
)

// All text that was typed after the command name is provided as input.
type UnstructuredCommandInput struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// A hint to display when the input hasn't been provided yet
	Hint string `json:"hint"`
}

// Context window and cost update for a session.
type UsageUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// Cumulative session cost (optional).
	Cost *Cost `json:"cost,omitempty"`
	// Total context window size in tokens.
	Size uint64 `json:"size"`
	// Tokens currently in context.
	Used uint64 `json:"used"`
}

// Request to wait for a terminal command to exit.
type WaitForTerminalExitRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The session ID for this request.
	SessionID SessionId `json:"sessionId"`
	// The ID of the terminal to wait for.
	TerminalID TerminalId `json:"terminalId"`
}

// Response containing the exit status of a terminal command.
type WaitForTerminalExitResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The process exit code (may be null if terminated by signal).
	ExitCode *int64 `json:"exitCode,omitempty"`
	// The signal that terminated the process (may be null if exited normally).
	Signal *string `json:"signal,omitempty"`
}

// Request to write content to a text file.
// Only available if the client supports the fs.writeTextFile capability.
type WriteTextFileRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
	// The text content to write to the file.
	Content string `json:"content"`
	// Absolute path to the file to write.
	Path string `json:"path"`
	// The session ID for this request.
	SessionID SessionId `json:"sessionId"`
}

// Response to fs/write_text_file
type WriteTextFileResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	// See protocol docs: Extensibility
	Meta Meta `json:"_meta,omitempty"`
}
